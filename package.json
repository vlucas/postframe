{
  "name": "postframejs",
  "version": "1.0.0",
  "description": "Simplified postMessage communication between frames",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "test": "jest",
    "prepack": "npm run clean && npm run build"
  },
  "keywords": [
    "postMessage"
  ],
  "author": "Vance Lucas <vance@vancelucas.com>",
  "homepage": "https://github.com/vlucas/postframe",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.4"
  }
}
